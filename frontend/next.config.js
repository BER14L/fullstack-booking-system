/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The API URL is exposed to the browser via NEXT_PUBLIC_API_URL; nothing
  // server-only is needed here for this project.
};

module.exports = nextConfig;
